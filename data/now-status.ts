/** Living snapshot. First line is the home chrome status. */
export const NOW = [
  'hub restyle — this site as a quiet personal hub',
  'Shipping and polishing mitori and ApplePackage on macOS / Swift.',
  'Small private tools — bookkeeping, WeChat posts, agent skills.',
] as const

export const NOW_STATUS = NOW[0]
export const NOW_LABEL = 'now'
